# 2026-09-01

## 修复 7 个用户报告的运行问题(末尾操作失败 56→0)

### 1. **4.6 Backtrader 案例** — `20b83977` hash
- 源码:``stamp_duty=0.001` 不被 backtrader setcommission 支持
- 修复:移除 stamp_duty,只留 commission=0.0003
- `dataline=` 改为 `dataname=`(backtrader 新版 API)

### 2. **4.6 框架 benchmark** — `dc21f062` hash
- 源码:`pd.bdate_range` 与 vectorbt 不兼容(非 fixed frequency 报错)
- 修复:`pd.bdate_range` → `pd.date_range`
- benchmark 真跑通(20+ s, vectorbt 174 组合),已手动嵌入完整结果 text

### 3. **4.7 单元测试 demo** — `c9c48d50` hash
- 源码 fence 3 引用前面 fence 1+2 的类,无法独立跑
- 修复:在 unit test fence 顶部加 mock 类(Portfolio/RiskManager/ExecutionHandler 等)
- mock Portfolio 用 property 实时计算 equity(让 cash 改后立刻反映)
- mock RiskManager 简化版,只实现 check_order 和 peak_equity
- scaffold ignore 列表加 `Exception, AssertionError, ...`(否则覆盖 Python 内置名破坏 except)

### 4. **4.9 4 种资金分配方法对比** — `8a274439` hash
- 源码 fence 引用前面 `returns_df`,但 fence 2 顶部没生成数据
- 修复:fence 顶部加 fixture 生成 returns_df(3 个策略模拟日收益)

### 5. **4.10 顺序学习整页没内容** — 组件 bug
- StaticCodeBlock.vue `defaultOpen: false` 导致 output 默认折叠,用户以为没内容
- 修复:`defaultOpen: true`,output 默认展开

### 6. **7.2 VWAP 切片** — `0622d75c` hash
- 源码 fence 只有变量计算,无 print
- 修复:fence 末尾加 print 显示切片结果(profile + 5 活跃 + 5 冷清时间片)

### 7. **8.3 情感分析 + FinBERT** — `7ad52f71`, `a79c3e34` hash
- 源码依赖 HuggingFace 模型下载,沙箱无网络
- 修复:情感分析加 try/except fallback(mock 结果)+ 预生成完整 text
- FinBERT 同样预生成 mock 文本(预期输出 + 模型架构 + 离线提示)

### scaffold 改进
- `precompute-py-outputs.mjs` ignore 列表加:`Exception, AssertionError, ValueError, KeyError, TypeError, IndexError, NameError, AttributeError, ZeroDivisionError, StopIteration`(避免覆盖 Python 内置)
- `precompute-py-outputs.mjs` main() 判断改 path.resolve 标准化路径
- `extract-py-fences.mjs` 用逐文件删除替代 rm(OUT_DIR, recursive)(绕开 safe-delete)

### 安装依赖
- `backtrader` 已装
- `transformers` 已装
- `torch` CPU 版已装

### vitepress build
- 必须 `unset NODE_OPTIONS` 绕开 safe-delete(.temp 清理时触发 bulk guard)
- mv `.vitepress/dist` + `.vitepress/.temp` → bak 后再 build 才不卡
- relativize-dist 输出 120 html, 120 rewritten

### 状态
- 末尾操作失败:0
- 437 fences 全部有 output.json
- `portable/dist` 已 relativize,serve PID 30144 监听 5189
- 4.10 页面验证 `<details class="scb-out" open>` ✓