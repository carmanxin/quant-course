# 2026-08-26

## 18:34 CST — QuantLab 监控 Run #89 (用户对时触发)

- 用户指令: `现在对时，晚上10点你再看下进度`
- 当前时间确认: **2026-08-26 (Wed) 18:34:32 CST**
- 距上次正式报告 Run #88 (8-25 17:55) 已 ~24h39m,远超 4h 阈值 → 触发正式检查

### 状态(与 Run #88 字节级一致)

- ❌ `portable/dist/guide/m01-overview/1.4-challenges.html` 缺失
- ❌ `portable/dist/` 仅 `code/` + `favicon.svg`,2.5M / **0 HTML**
- ❌ 5 端口全停: `:5189/5188/5173/5174/5175` 全部 HTTP 000 (curl exit 7)
- ❌ netstat `:51xx` LISTENING = 0;QuantLab serve.cjs 进程 = 0
- ✅ `.vitepress/_dist_old/` 52M / 122 HTML / `1.4-challenges.html` 149,056 B 完整保留
- ✅ 源 `guide/m01-overview/1.4-challenges.md` 17,562 B 健康
- git HEAD 冻结 `c881a64` (Aug 12),git dirty 521 文件未提交

### 本轮新增发现

1. `portable/serve.cjs` 默认端口已从 5189 改为 **5173**,支持 `--port` 自定义
2. `portable/README.md` 头部声明 "完全离线运行 · 拷贝即用" — 新架构目标运行模型是 **file:// 直开**,不再依赖常驻 Node 服务
3. `git log` 自 7-17 后发生 14 次重构 commit,关键节点:
   - `9e7f071` 去除 pyodide
   - `bce8ada` 引入构建期 precompute (生成 `.output.json` + `.svgs`)
   - `6618983` precompute 接入 `npm run build`
   - `54b0c45` StaticCodeBlock SSR inline(支持 file://)
   - `c881a64` 路径相对化(latest HEAD)

### 累计宕机

- 首次: Run #6 (8-10 11:33)
- 当前: Run #89 (8-26 18:34)
- 累计 **~387h+ ≈ 16.2 天**,83 轮连续监控症状

### 监控脚本规则失效点

旧规则硬编码检查 `portable/dist/guide/m01-overview/1.4-challenges.html` + 5189 端口,在新架构下永远报错。需要按 `c881a64` 后的模型升级(检查 `.vitepress/dist/` 或 `portable/dist/` 在构建后产物 + `serve.cjs` 进程),否则只会永远报"dist 缺 HTML"。

### 用户决策待办

- (a) `cp -r .vitepress/_dist_old/* portable/dist/` + `node portable/serve.cjs --port 5189` (最快 ~30s 恢复)
- (b) `git stash` 521 modified → `npm run build` 完整重建 (~5-10 min)
- (c) 调查为何 16+ 天无恢复尝试

### 下一轮

目标 **2026-08-26 22:00 CST (Run #90)**,用户指令明确指定。