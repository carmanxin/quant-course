# 跳过 Playwright 浏览器下载：
# playwright 只在本地辅助脚本 scripts/_cmp-shot.mjs 中用到，
# 但它的 postinstall 会下载 ~150MB Chromium —— 在 CI / 托管平台的构建容器里
# 这是最常见的「安装依赖失败」原因。设为 1 只装 JS 包、不下浏览器。
playwright_skip_browser_download=1

# 少打日志、跳过资金与审计请求，安装更快更稳
fund=false
audit=false
progress=false
